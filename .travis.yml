language: java

matrix:
  include:
    - os: linux
      sudo: required
      jdk: openjdk8
    - os: linux
      sudo: required
      jdk: oraclejdk9

script:
    chmod +x ./gradlew
    ./gradlew clean build

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/